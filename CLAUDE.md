# CLAUDE.md

## Tooling

- This project uses **Bun**. Always use `bunx` instead of `npx` (e.g. `bunx tsc --noEmit`, `bunx prisma migrate deploy`).
- Do not add excessive comments to the code and only add code if it is actually necessary to explain.
